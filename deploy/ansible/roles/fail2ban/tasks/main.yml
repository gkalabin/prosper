---
- name: Install fail2ban
  apt:
    name: fail2ban
    state: present
    update_cache: true

- name: Deploy jail config
  template:
    src: jail.local.j2
    dest: /etc/fail2ban/jail.local
    mode: '0644'
  notify: restart fail2ban

- name: Override nginx-bad-request filter to spare app 400s
  template:
    src: nginx-bad-request.local.j2
    dest: /etc/fail2ban/filter.d/nginx-bad-request.local
    mode: '0644'
  notify: restart fail2ban

- name: Enable and start fail2ban
  service:
    name: fail2ban
    state: started
    enabled: true
