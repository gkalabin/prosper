all:
  hosts:
    prosper:
      ansible_host: 192.168.1.100 # Pi LAN IP, or the VM's external IP
      ansible_user: your_user
      # ansible_port: 2223                              # if you set a non-default ssh_port
      # ansible_ssh_private_key_file: ~/.ssh/your_key   # if not your default key / ssh-agent
