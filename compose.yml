version: '3'

services:
  fe:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: gkalabin/prosper
    ports:
      - '3000:3000'
    volumes:
      - .env:/app/.env

  db:
    image: mysql
