root = "."
tmp_dir = "tmp"

[build]
  pre_cmd = ["cd ../proto && buf generate"]
  cmd = "go build -o ./tmp/backend ./cmd/backend"
  bin = "./tmp/backend"
  include_ext = ["go", "proto"]
  include_dir = [".", "../proto"]
  exclude_dir = ["tmp", "gen"]
  delay = 1000

[log]
  time = true

[misc]
  clean_on_exit = true
